<% layout("layouts/boilerplate") %>
<h3>Rooms</h3>
<div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1">
  <% for(let house of houses){ %>
  <a href="/houses/<%= house._id %>" class="listing-link">
    <div class="card col listing-card-3d listing-c">
      <img
        src="<%= house.image.url %>"
        class="card-img-top"
        alt="room_image"
        style="height: 20rem"
        
      />
      <div class="card-img-overlay"></div>
      <div class="card-title">
        <p class="card-title">
          <b>&nbsp;&nbsp;<%= house.title %></b> <br />
          <% if (house.price != null) { %>
            &nbsp;&nbsp;&#8377;<%= house.price.toLocaleString("en-IN") %>/night <br />
          <% } else { %>
            Price not available <br />
          <% } %>
          <br />
          &nbsp;&nbsp;<%= house.description %> <br />
        </p>
      </div>
    </div>
  </a>
  <% } %>
</div>
